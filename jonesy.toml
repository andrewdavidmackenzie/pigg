allow = ["format", "capacity", "unknown", "oom", "misaligned_ptr"]

[[rules]]
path = "**/iroh_device.rs"
allow = ["invalid_enum"]

[[rules]]
path = "**/tcp_device.rs"
allow = ["invalid_enum"]

[[rules]]
path = "**/piggpio/src/config.rs"
allow = ["invalid_enum"]
